# Test Root

Valid root instruction file under 150 lines with npm script references.

Run `npm run check` before committing.

## Scope and Precedence

This file applies repository-wide.
Nested AGENTS.md files add rules for their subtree.

## Verification

Run `npm run check:format` for formatting checks.

## Git Conventions

Never commit unless explicitly asked.
Use `--force-with-lease`, never `--force`.

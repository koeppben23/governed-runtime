// Canonical reason codes — the single source of truth for all error reasons.
// Do not create local duplicate reason code definitions.

export const REASONS = {
  EXPIRED_EVIDENCE: 'evidence-expired',
} as const;
